#include <stdio.h>
#include<Windows.h>
#include<string.h>
#include<conio.h>  
#include<stdlib.h>

int map[100][100];
for (y0 = 0; i < 24; i++)
{
	for (x0= 0; i < 41; i++)
	{
		if ((x0 == 0||x0==1||x0==39||x0==41))
		{
			map[x0][y0] = 0;
			
		}
		else if (y0 == 0)
		{
			map[x0][y0] = 1;
		}
		if(x)
	}
}
